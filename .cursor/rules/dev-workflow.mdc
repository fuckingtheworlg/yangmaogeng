# 项目特有开发规范（羊毛梗船舶服务中心）

本文件只记录**本项目特有的踩坑和约定**。通用 AI 协作原则见全局规则：

- 工作契约：`~/.cursor/rules/ai-working-contract.mdc`
- 排查方法论：`~/.cursor/rules/debug-methodology.mdc`
- 进度文档：`~/.cursor/rules/progress-doc.mdc`

**优先级**：当本文件与全局规则冲突时，本文件覆盖。

---

## 1. Mock-to-API 清零检查（项目特有操作）

后端 API 变更完成后，必须跑一遍残留扫描：

```bash
bash scripts/lint-residuals.sh
```

该脚本会检测：硬编码 IP、`localhost:3000` 硬编码、mock 残留、空 catch 块、console.log 残留。

**两端都要查**：小程序端（`miniprogram/`）和管理后台（`admin/`）同改同验。`api/admin.js` 或 `api/index.js` 中定义了但没被 import 的函数 = 未接入的信号。

## 2. 平台级 API 失败：本项目专用的微信小程序检查清单

遇到微信小程序 API 报错或静默失败时，除全局 `debug-methodology.mdc` 第 3 节的通用顺序外，**本项目已确认踩过的具体坑**：

### 2.1 隐私协议相关 API（chooseAvatar / nickname / getUserProfile 等）

1. `miniprogram/app.json` 是否配 `"__usePrivacyCheck__": true`
2. 微信后台「用户隐私保护指引」是否勾选了所有用到的 API 接口
3. 代码中是否有 `wx.getPrivacySetting` + `agreePrivacyAuthorization` 的处理流程

**禁止**用 `type="text"` 替换 `type="nickname"` 绕过平台 API。

### 2.2 域名相关

- 所有 `wx.request` / `wx.uploadFile` 的目标域名**必须**在小程序后台「服务器域名」白名单中
- 白名单要求 HTTPS，服务器必须先有 SSL 证书（ICP 备案 ≠ SSL，详见 `docs/progress/deploy.md`）

## 3. 数据容错：JS/本项目特化补充

全局 `debug-methodology.mdc` 第 6 节已覆盖 null/undefined/类型边界的通用守则。**本项目后端专有**：

### 3.1 写数据库前统一序列化函数

后端每个写 DB 的 POST/PUT 必须遵循：

```js
// null / undefined → SQL NULL（或 PUT 时跳过该字段）
// 数组 / 对象 → JSON.stringify
// 其他 → 原样
if (v === null || v === undefined) return null
if (Array.isArray(v) || typeof v === 'object') return JSON.stringify(v)
return v
```

关键点：`typeof null === 'object'`，必须**先排 null**。

### 3.2 前端 `{ ...row, ... }` 的陷阱

管理后台 `form = { ...row, ... }` 会把后端返回的 null、时间字符串、多余字段一起带回提交。对策：

- 列白名单明确要提交的字段
- 数值字段 edit 时用 `parseFloat(row.x) || 0` 兜底
- 后端对 null/undefined 幂等处理（视为不更新）

### 3.3 Schema 迁移后必须测历史数据（本项目已累计 4 次 migration）

`ALTER TABLE ADD COLUMN` 之后，旧行新字段可能是 NULL（即使列带 DEFAULT）。必须抽样一条**迁移前**就存在的行，走完整「读 → 展开 → 修改 → 保存」流程。

## 4. 构建产物提交前验证（本项目 `admin/dist/` 进 git）

本项目 `.gitignore` 对 `admin/dist/` 做了白名单（`!admin/dist/`），**前端构建产物要 commit 到 git**（服务器 pull 更新，不在服务器构建）。

每次 `npm run build` 后：

```bash
git status                              # 确认 dist/ 变更在变更列表
git check-ignore -v <file>              # 如果没出现，查为何被忽略
git add -f admin/dist                   # 被误忽略时强制添加
git show --stat HEAD                    # 提交后验证文件数量
```

## 5. 部署后三层验证（本项目三端联动，缺一不可）

每次 push 到服务器后：

1. **API 层**：`curl https://yangmaogeng.top/api/ships` 返回正确 JSON
2. **管理后台**：浏览器访问 `https://yangmaogeng.top/` 渲染正常，数据与 curl 一致
3. **小程序端**：微信开发者工具清缓存+真机预览，数据与上两层一致

或直接跑：`bash test-scripts/run-all-tests.sh`（API 层自动化）。

## 6. 进度文档维护（本项目模块映射）

**全局规则**：`~/.cursor/rules/progress-doc.mdc`（触发条件、四段式结构、禁止行为）。

**本项目已有模块**（对应 `docs/progress/` 下的文件）：

| 代码位置 | 文档 |
|---|---|
| `server/routes/ships.js` / `admin/src/views/ship/` / `miniprogram/pages/detail` | `docs/progress/ship.md` |
| `server/routes/commissions.js` / `admin/src/views/commission/` | `docs/progress/commission.md` |
| `server/routes/transactions.js` / `admin/src/views/transaction/` | `docs/progress/transaction.md` |
| `server/routes/wx.js` / `admin/src/views/user/` | `docs/progress/user.md` |
| `server/routes/favorites.js` | `docs/progress/favorites.md` |
| `server/routes/upload.js` / `admin/src/components/ImageUploader.vue` | `docs/progress/upload.md` |
| `server/config/migration-*.sql` | `docs/progress/schema.md` |
| `server/deploy.sh` / `server/setup-*.sh` | `docs/progress/deploy.md` |

**本项目允许跳过文档的情况**（对应全局规则 5. 元规则的项目实例化）：

- 单行语法兼容修复（如 Nginx 版本差异、lint 规则调整）
- 构建产物重新生成（`admin/dist/`）
- 文档本身的修订
- 纯重命名（变量/文件名）且不改语义

**即使跳过文档，commit message 必须写清根因。** 若该改动是全局规则"修复非显而易见 bug"（调试超过 30 分钟）的情形，则**不允许**跳过，必须写「踩坑记录」段。

## 7. 会话起手动作

开始一个涉及已有模块的任务前，先读对应的 `docs/progress/<模块>.md`，确认不重复已完成工作、不违反既有决策。

若本次改动会影响**后端地址 / API 契约**，提交前跑一次 `bash scripts/lint-residuals.sh`。
