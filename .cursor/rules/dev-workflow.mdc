---
description: 开发流程防错规范 — mock 替换、问题排查、平台 API、数据容错、构建提交、部署验证
alwaysApply: true
---

# 开发流程防错规范

## 1. Mock-to-API 清零检查

后端 API 开发完成后，必须搜索所有前端代码中残留的 mock 引用：

```bash
rg "mock" --type vue --type js --type ts -l
```

- 逐个确认每处 mock 是否已替换为真实 API 调用
- 小程序端和管理后台两端都要检查，不能只改一端
- API 函数文件（如 `api/admin.js`）中已定义但未被 import 的函数 = 未接入的信号

## 2. 问题排查：先证据后结论

遇到"数据不一致"、"页面空白"等问题时，严格按顺序执行：

1. **读源码** — 确认数据来源是 API 还是 mock/本地存储
2. **查 API 响应** — curl 直接请求接口，确认后端返回值
3. **查日志** — 后端日志、浏览器 Console、Nginx error.log
4. **得出结论** — 基于以上证据定位原因

禁止在没有证据的情况下猜测"是缓存问题"或"是网络问题"。

## 3. 平台级 API 失败：先查平台配置，再改代码

使用微信小程序、支付宝小程序等平台的原生 API 时，如果 API 报错或静默失败：

**排查优先级**（从上到下依次排查，不可跳过）：

1. **平台后台配置** — 隐私协议声明、API 权限开通、域名白名单
2. **AppID / 基础库版本** — 当前 AppID 是否有该 API 权限，基础库版本是否满足最低要求
3. **API 调用时序** — 是否需要先授权再调用（如隐私协议同意 → chooseAvatar）
4. **代码层面** — 事件绑定、参数传递、返回值处理

**禁止行为**：

- 禁止在未确认平台配置的情况下，用 `type="text"` 替换 `type="nickname"` 等方式绕过平台 API
- 禁止看到 API "能用"就跳过同类 API 检查（如 chooseAvatar 成功不代表 nickname 也无需配置）
- 禁止将 `Error: timeout` 类平台内部错误归因为"网络问题"或"组件兼容性"

**微信小程序隐私协议检查清单**：

- `app.json` 中是否配置 `"__usePrivacyCheck__": true`
- 微信后台"用户隐私保护指引"中是否勾选了所有用到的 API 接口
- 代码中是否有 `wx.getPrivacySetting` + `agreePrivacyAuthorization` 处理流程

## 4. 数据容错与历史数据测试

### 4.1 JS 类型陷阱

`typeof null === 'object'`，所以"对象就序列化"这种判断永远要**先排 null**：

```js
// 错
if (typeof v === 'object') JSON.stringify(v)   // null → '"null"' 字符串炸 INT 列

// 对
if (v === null || v === undefined) return null
if (Array.isArray(v) || typeof v === 'object') return JSON.stringify(v)
return v
```

后端每个写数据库的 POST/PUT 必须遵循：
- `null / undefined` → SQL NULL（或 PUT 时跳过该字段）
- 数组/对象 → `JSON.stringify`
- 其他 → 原样

### 4.2 Schema 迁移后必须测历史数据

每次 `ALTER TABLE ADD COLUMN` 之后：

1. 新数据字段值完整（测试永远 happy path）
2. **旧数据可能是 NULL**（即使列带 DEFAULT 0，个别行可能因执行时序或历史操作遗留 NULL）
3. 必须至少抽样编辑一条 migration 之前就存在的旧行，完整走一遍"读取 → 展开 → 修改 → 保存"

禁止只用"新增的数据"测完就交付。

### 4.3 `...row` 展开要警惕

前端 `form = { ...row, ... }` 会把后端返回的 null、时间字符串、多余字段一起带回去提交。做法：
- 能列白名单就只发白名单字段
- 或者后端对 null/undefined 做幂等处理（视为不更新）
- 数值字段（price/width/...）在 edit 时用 `parseFloat(row.x) || 0` 兜底，避免字符串混入

### 4.4 错误必须可见

后端所有 `catch (err)` **必须** `console.error(err)` 带 stack 打到日志，否则 journalctl 只看得到重启信息看不到错误。

前端 `catch` 的 toast 必须带后端 message：

```js
ElMessage.error(res?.message || err?.response?.data?.message || '操作失败')
```

开发/预发环境，后端应把具体 SQL / 字段错误返回前端，生产环境才脱敏。

## 5. 构建产物提交前验证

每次重新构建 dist/ 并提交时：

1. 构建完成后执行 `git status`，确认新文件出现在变更列表中
2. 如果新文件未出现，执行 `git check-ignore -v <file>` 检查是否被 .gitignore 排除
3. 被排除时使用 `git add -f <path>` 强制添加
4. 提交后执行 `git show --stat HEAD`，确认文件数量符合预期

## 6. 部署后端到端验证

每次推送到服务器后，三层验证缺一不可：

1. **API 层** — `curl http://localhost:3000/api/xxx` 确认返回正确 JSON
2. **管理后台** — 浏览器访问确认页面渲染正常、数据与 curl 结果一致
3. **小程序端** — 确认小程序显示的数据与 API 返回一致

三层数据全部一致才算部署完成。
