export const mockShips = Array.from({ length: 12 }, (_, i) => ({
  id: i + 1,
  ship_no: `2023041011${String(i + 1).padStart(4, '0')}`,
  total_length: 80 + Math.floor(Math.random() * 80),
  width: 10 + Math.floor(Math.random() * 12),
  depth: 4 + Math.floor(Math.random() * 8),
  ship_type: ['川船', '海船', '散货船', '集装箱船'][i % 4],
  ship_condition: ['优秀', '良好', '一般'][i % 3],
  deadweight: [3000, 5000, 8000, 12000, 15000][i % 5],
  gross_tonnage: [900, 1500, 2000, 3500, 5000][i % 5],
  build_date: `202${i % 6}-${String((i % 12) + 1).padStart(2, '0')}`,
  build_province: ['南京', '上海', '重庆', '武汉', '广州', '大连'][i % 6],
  port_registry: ['南京', '上海', '重庆', '武汉', '广州', '大连'][i % 6],
  engine_brand: ['潍柴', '玉柴', 'MAN', '潍柴'][i % 4],
  engine_power: [800, 1200, 1500, 2000, 3000][i % 5],
  water_type: i % 2 === 0 ? '内河' : '近海',
  price: [800, 1200, 1500, 2000, 2500, 3000, 3500, 5000][i % 8],
  contact_name: `联系人${i + 1}`,
  contact_phone: `136${String(i).padStart(8, '0')}`,
  status: i % 3 === 2 ? 0 : 1,
  images: [],
  certificates: []
}))

export const mockUsers = Array.from({ length: 12 }, (_, i) => ({
  id: i + 1,
  openid: `wxid_${String(i).padStart(8, '0')}`,
  nickname: `用户${i + 1}`,
  phone: `138${String(i).padStart(8, '0')}`,
  commission_ids: i < 6 ? `C${String(i + 1).padStart(4, '0')}` : '',
  favorite_ids: `S${String(i + 1).padStart(4, '0')}`,
  created_at: `2025-0${(i % 9) + 1}-${String((i % 28) + 1).padStart(2, '0')}`
}))

export const mockCommissions = Array.from({ length: 12 }, (_, i) => ({
  id: i + 1,
  user_id: i + 1,
  type: i % 2 === 0 ? 'sell' : 'buy',
  contact_name: `${['王', '李', '张', '刘', '陈', '杨'][i % 6]}${'先生女士'[i % 2]}`,
  gender: i % 2 === 0 ? '先生' : '女士',
  phone: `139${String(i).padStart(8, '0')}`,
  total_length: 60 + i * 8,
  width: 10 + i,
  depth: 4 + i * 0.5,
  deadweight: 3000 + i * 1000,
  gross_tonnage: 900 + i * 300,
  build_date: `202${i % 6}-0${(i % 9) + 1}`,
  build_province: ['南京', '上海', '重庆', '武汉', '广州', '大连'][i % 6],
  water_type: i % 2 === 0 ? '内河' : '近海',
  ship_type: ['川船', '海船', '散货船'][i % 3],
  engine_brand: ['潍柴', '玉柴', 'MAN'][i % 3],
  engine_power: 800 + i * 200,
  budget: i % 2 === 1 ? 1000 + i * 500 : null,
  price: i % 2 === 0 ? 1000 + i * 500 : null,
  ship_images: [],
  cert_images: [],
  remark: '',
  status: i % 3,
  created_at: `2025-04-${String((i % 28) + 1).padStart(2, '0')}`
}))

export const mockTransactions = Array.from({ length: 12 }, (_, i) => ({
  id: i + 1,
  ship_id: i + 1,
  ship_no: `2023041011${String(i + 1).padStart(4, '0')}`,
  commission_id: i < 6 ? i + 1 : null,
  price: 800 + i * 300,
  buyer_name: `买家${i + 1}`,
  buyer_phone: `137${String(i).padStart(8, '0')}`,
  seller_name: `卖家${i + 1}`,
  seller_phone: `136${String(i).padStart(8, '0')}`,
  total_length: 80 + i * 5,
  width: 10 + i,
  depth: 5 + i * 0.5,
  deadweight: 3000 + i * 1000,
  gross_tonnage: 900 + i * 300,
  build_date: `202${i % 6}-0${(i % 9) + 1}`,
  engine_brand: ['潍柴', '玉柴', 'MAN'][i % 3],
  engine_power: 800 + i * 200,
  water_type: i % 2 === 0 ? '内河' : '近海',
  remark: '',
  deal_date: `2025-04-${String((i % 28) + 1).padStart(2, '0')}`,
  created_at: `2025-04-${String((i % 28) + 1).padStart(2, '0')}`
}))
